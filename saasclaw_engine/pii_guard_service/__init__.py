"""
PII Guard microservice — Presidio-based PII detection and sanitization.

Run as a standalone FastAPI service on localhost:8900.
Consumed by the Python engine, Pi coding agent (TS), and studio views.
"""
